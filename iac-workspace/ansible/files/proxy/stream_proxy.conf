log_format stream_log "$time_local $remote_addr -> $upstream_addr [$status]";
access_log /var/log/nginx/stream_access.log stream_log;

server {
    listen 443;
    proxy_pass 100.116.167.59:443;
    proxy_protocol on;
}
