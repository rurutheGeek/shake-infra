global:
  resolve_timeout: 1m

route:
  receiver: 'failover_webhook'
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: 'failover_webhook'
    webhook_configs:
      - url: 'http://127.0.0.1:9000/webhook'